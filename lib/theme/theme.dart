import 'package:flutter/material.dart';

class AppTheme {
  // Accent colors
  static const Color accentColor = Color(0xFF5E9EFF);
  static const Color accentBlue = Color(0xFF4A90E2);
  
  // Background colors
  static const Color background = Color(0xFFF5F7FA); // Lighter blue-purple for main content
  static const Color background2 = Color(0xFF1A1F3A); // Dark blue-purple for sidebar
  static const Color backgroundDark = Color(0xFF25254B);
  
  // Text colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textWhite = Color(0xFFFFFFFF);
  
  // Shadow
  static const Color shadow = Color(0xFF4A5367);
  static const Color shadowDark = Color(0xFF000000);
  
  // Menu selection
  static const Color menuSelected = Color(0xFF5E9EFF);
  static const Color menuHover = Color(0xFF2A3F5F);
}

